{% extends "base.html" %}
{% load sport_extras %}

{% block bodytitle %}
    <h1 class="body_title">{{sport.display}}</h1>
{% endblock %}


{% block body_content %}


  <div id="myCarousel" class="carousel slide">

        <ul class="nav nav-pills">
          <div class="btn-group">	  
            <button id="carousel_group_A"><a href="#myCarousel" id="carousel_group_A">Group A</a></button>
            <button id="carousel_group_B"><a href="#myCarousel" id="carousel_group_B">Group B</a></button>
          </div>
	</ul>

      <div class="carousel-inner">
        <div class="active item">
          <table class="table table-bordered">
            <thead class="sport_brand">

              <tr>
                <th class="tablehead_width_fraternity">Fraternity</th>
                <th class="tablehead_width_sport">Wins</th>
                <th class="tablehead_width_sport">Losses</th>
                <th class="tablehead_width_sport">Ties</th>
              </tr>

            </thead>
            <tbody>
              {% for fraternity in sport|group:"A" %}

              <tr>
                <td>{{fraternity.name}}</td>
              </tr>

              {% endfor %}
            </tbody>
          </table>
        </div>

        <div class="item">
          <table class="table table-bordered">
            <thead class="sport_brand">

	      <tr>
                <th class="tablehead_width_fraternity">Fraternity</th>
                <th class="tablehead_width_sport">Wins</th>
                <th class="tablehead_width_sport">Losses</th>
                <th class="tablehead_width_sport">Ties</th>
              </tr>

            </thead>
            <tbody>
               {% for fraternity in sport|group:"B" %}

               <tr>
                 <td>{{fraternity.name}}</td>
               </tr>

               {% endfor %}
            </tbody>
          </table>
         </div>
        </div>
     </div>


{% endblock %}

