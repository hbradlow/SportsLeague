



{% load staticfiles %}
{% load sport_extras %}
<!DOCTYPE html>
<html lang="{{ LANGUAGE_CODE }}">
    <head>
        <meta charset="utf-8" />
        <title>{% block head_title_base %}{% if SITE_NAME %}{{ SITE_NAME }} | {% endif %}{% block head_title %}{% endblock %}{% endblock %}</title>
        {% block viewport %}
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {% endblock %}
        {% block style_base %}
            <link href="{% static "bootstrap/less/sports_tracker.css" %}" rel="stylesheet">
            <link href="{% static "bootstrap/css/bootstrap-responsive.css" %}" rel="stylesheet">

          {% block extra_style %}{% endblock %}
        {% endblock %}
        <!-- HTML5 shim, for IE6-8 support of HTML elements -->
        <!--[if lt IE 9]>
            <script src="https://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->
        {% block extra_head_base %}
            {% block extra_head %}{% endblock %}
        {% endblock %}
    </head>

    <body class="{% block body_class %}{% endblock %}" id="{% block body_id %}{% endblock %}">

        {% block topbar_base %}

            <div class="navbar {% block navbar_class %}navbar-fixed-top{% endblock %}">

                <div class="navbar-inner">
                    <div class="container" id="header">
                        {% block topbar %}
                            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>

                            </a>
                            {% block site_brand %}<a class="brand" href="{% url home %}">Sports League</a>{% endblock %}

                            <div class="nav-collapse">
                                {% block nav %}
                                {% endblock %}
                                {% block account_bar %}{% endblock %}
                            </div>
                        {% endblock %}

                    </div>

                </div>

            </div>


        {% endblock %}
        {% block body_base %}

        <div class="container" style="margin-top:80px;">  
	{% include "_messages.html" %}
	  <div class="row">
	    <div class="span10">
	      {% block bodytitle %}
	      {% endblock %}
	    </div>
	  </div>
	    {% block sidebar %}
	    <div class="span2 side_bar" style="border:2px solid #1a1914">
             {% block sidebar_content %}
			   <div class="sidebar-nav-fixed">
                  <ul class="nav nav-list">
                    <li align="center" class="side_bar_sport"><a href="{% url home %}" class="black">Overall Rankings</a></li>
                    <li class="nav-header quarter">Fall</li>
                    {% for sport in sports|season:"FL" %}
		         {% if sport.is_major %}
                            <li align="center" class="side_bar_sport"><a href="{% url sport_detail sport.slug %}">{{sport.display}}</a></li>
			    {% if not forloop.last %}
                                <hr />
                            {% endif %}
			 {% endif %} 	
                    {% endfor %}
				<hr />
			    <li align="center" class="dropdown side_bar_sport">
			      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
				Minor Sports

				<b class="caret"></b>
			      </a>
			      <ul class="dropdown-menu side_bar">
				{% for sport in sports|season:"FL" %}
				  {% if not sport.is_major %}
				    <li align="center" class="side_bar_sport">{{sport.display}}</li>
				    {% if not forloop.last %}
                                    <hr />
				    {% endif %}
				  {% endif %}
				{% endfor %}
			      </ul>
			    </li>
	                    <li class="nav-header quarter">Winter</li>
                    {% for sport in sports|season:"WR" %}
			 {% if sport.is_major %}   
                            <li align="center" class="side_bar_sport"><a href="{% url sport_detail sport.slug %}">{{sport.display}}</a></li>
                            {% if not forloop.last %}
                                <hr />
                            {% endif %}
			 {% endif %}
                    {% endfor %}
				<hr />
                            <li align="center" class="dropdown side_bar_sport">
			            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                Minor Sports
                                <b class="caret"></b>
                              </a>
                              <ul class="dropdown-menu side_bar">
				{% for sport in sports|season:"WR" %}
                                  {% if not sport.is_major %}
                                    <li align="center" class="side_bar_sport">{{sport.display}}</li>
                                    {% if not forloop.last %}
                                    <hr />
                                    {% endif %}
                                  {% endif %}
                                {% endfor %}
                              </ul>
                            </li>
                    <li class="nav-header quarter">Spring</li>
                    {% for sport in sports|season:"SG" %}
                        {% if sport.is_major %}    
		          <li align="center" class="side_bar_sport"><a href="{% url sport_detail sport.slug %}">{{sport.display}}</a></li>
                            {% if not forloop.last %}
                                <hr />
                            {% endif %}
		        {% endif %}
                    {% endfor %}
				<hr />
                            <li align="center" class="dropdown side_bar_sport">
			            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                Minor Sports
                                <b class="caret"></b>
                              </a>
                              <ul class="dropdown-menu side_bar">
				{% for sport in sports|season:"SG" %}
                                  {% if not sport.is_major %}
                                    <li align="center" class="side_bar_sport">{{sport.display}}</li>
                                    {% if not forloop.last %}
                                    <hr />
                                    {% endif %}
                                  {% endif %}
                                {% endfor %} 
                              </ul>
                            </li>
			      </ul>
			   </div>

			 {% endblock %}
             </div>
             {% endblock %}
	     {% block body %}
	     <div class="span8" stye="border:2px solid #000">
                {% block body_content %}
                {% endblock %}
	     </div>
	     {% endblock %}
	</div>
        {% endblock %}
        {% block footer_base %}
            <div id="footer">
                <div class="inner">
                    <div class="container">
                        {% block footer %}{% endblock %}
                    </div>
                </div>
            </div>
        {% endblock %}
        {% block script_base %}
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.1/jquery.min.js"></script>
            <script src="{% static "bootstrap/js/bootstrap.js" %}"></script>
	    <script src="{% static "bootstrap/js/sports_tracker5.js" %}"></script>
	    <script src="twitter-bootstrap-v2/docs/assets/js/bootstrap-transition.js"></script>
	    {% block extra_script %}{% endblock %}
        {% endblock %}
        {% block extra_body_base %}
            {% block extra_body %}{% endblock %}
        {% endblock %}
    </body>
</html>
